# Page Tools

A bookmarklet cockpit for any tab: video overlays, page helpers, inspect, and privacy.

Open `index.html` in a browser (or serve this folder). Drag **Drag to bookmarks bar** onto the bookmarks bar, then click it on any site.
